---
import projectData from '../../data/projects/projects.json';
import type { Locale, TranslationSchema } from '../i18n/translations';

interface Props {
  t: TranslationSchema;
  locale: Locale;
}

const { t, locale } = Astro.props;
const source = locale === 'es' ? projectData.es : projectData.en;
const toAnchor = (value: string) =>
  value
    .toLowerCase()
    .normalize('NFD')
    .replace(/[\u0300-\u036f]/g, '')
    .replace(/[^a-z0-9]+/g, '-')
    .replace(/(^-|-$)/g, '');
const copy =
  locale === 'es'
    ? {
        open: 'Ver mas',
        close: 'Cerrar',
        stack: 'Tecnologias',
        detailsLabel: 'Detalle de proyecto',
      }
    : {
        open: 'See more',
        close: 'Close',
        stack: 'Technologies',
        detailsLabel: 'Project details',
      };
---

<section id="projects">
  <h2>{t.projects.title}</h2>

  <div class="project-grid">
    {source.freelance.map((project, index) => (
      <>
        <button
          type="button"
          class="project-card"
          id={`project-${toAnchor(project.name)}`}
          data-open-project-dialog={`project-dialog-${index}`}
        >
          <h3>{project.name}</h3>
          <p class="type">{project.type}</p>
          <p>{project.description}</p>

          <ul class="highlights">
            {project.highlights.slice(0, 1).map((highlight) => (
              <li>{highlight}</li>
            ))}
          </ul>

          <p class="cta">{copy.open}</p>
        </button>

        <dialog id={`project-dialog-${index}`} aria-label={copy.detailsLabel}>
          <article class="dialog-content">
            <button
              type="button"
              class="dialog-close"
              data-close-project-dialog
              aria-label={copy.close}
              title={copy.close}
            >
              X
            </button>

            <h3>{project.name}</h3>
            <p class="type">{project.type}</p>
            <p>{project.description}</p>

            <ul class="highlights dialog-highlights">
              {project.highlights.map((highlight) => (
                <li>{highlight}</li>
              ))}
            </ul>

            <p class="stack-title">{copy.stack}</p>
            <ul class="tech">
              {[...new Set(project.techStack)].map((item) => (
                <li>{item}</li>
              ))}
            </ul>
          </article>
        </dialog>
      </>
    ))}
  </div>
</section>

<script is:inline>
  (() => {
    const lockScroll = () => {
      const current = Number(document.body.dataset.dialogOpenCount ?? '0');
      const next = current + 1;
      document.body.dataset.dialogOpenCount = String(next);
      document.body.style.overflow = 'hidden';
    };

    const unlockScroll = () => {
      const current = Number(document.body.dataset.dialogOpenCount ?? '0');
      const next = Math.max(0, current - 1);

      if (next === 0) {
        delete document.body.dataset.dialogOpenCount;
        document.body.style.overflow = '';
      } else {
        document.body.dataset.dialogOpenCount = String(next);
      }
    };

    const openButtons = document.querySelectorAll('[data-open-project-dialog]');

    openButtons.forEach((button) => {
      button.addEventListener('click', () => {
        const dialogId = button.getAttribute('data-open-project-dialog');
        if (!dialogId) return;
        const dialog = document.getElementById(dialogId);
        if (dialog instanceof HTMLDialogElement && !dialog.open) {
          dialog.showModal();
          lockScroll();
        }
      });
    });

    const closeButtons = document.querySelectorAll('[data-close-project-dialog]');

    closeButtons.forEach((button) => {
      button.addEventListener('click', () => {
        const dialog = button.closest('dialog');
        if (dialog instanceof HTMLDialogElement) {
          dialog.close();
        }
      });
    });

    const dialogs = document.querySelectorAll('dialog[id^="project-dialog-"]');

    dialogs.forEach((dialog) => {
      dialog.addEventListener('close', () => {
        unlockScroll();
      });

      dialog.addEventListener('click', (event) => {
        const bounds = dialog.getBoundingClientRect();
        const isInside =
          event.clientX >= bounds.left &&
          event.clientX <= bounds.right &&
          event.clientY >= bounds.top &&
          event.clientY <= bounds.bottom;

        if (!isInside) {
          dialog.close();
        }
      });
    });
  })();
</script>

<style>
  #projects {
    display: grid;
    gap: 0.85rem;
  }

  .project-grid {
    display: grid;
    gap: 0.75rem;
    grid-template-columns: 1fr;
  }

  .project-card {
    width: 100%;
    text-align: left;
    border: 1px solid var(--border);
    border-radius: 0.8rem;
    padding: 0.9rem;
    background: color-mix(in srgb, var(--bg) 92%, var(--accent) 8%);
    cursor: pointer;
    transition: transform 0.18s ease, border-color 0.18s ease;
    scroll-margin-top: 6.5rem;
  }

  .project-card:hover {
    transform: translateY(-2px);
    border-color: color-mix(in srgb, var(--accent) 40%, var(--border) 60%);
  }

  .project-card h3 {
    margin: 0;
    font-size: 1.05rem;
  }

  .type {
    margin: 0.25rem 0 0.55rem;
    color: var(--muted);
    font-size: 0.92rem;
  }

  .highlights {
    margin: 0.6rem 0;
    padding-left: 1.1rem;
    display: grid;
    gap: 0.3rem;
  }

  .cta {
    margin-top: 0.7rem;
    font-weight: 600;
    color: var(--accent);
  }

  dialog {
    border: 1px solid var(--border);
    background: var(--surface);
    color: var(--text);
    width: min(680px, 92vw);
    border-radius: 1rem;
    padding: 0;
  }

  dialog::backdrop {
    background: rgba(5, 8, 15, 0.65);
  }

  .dialog-content {
    padding: 1rem;
    display: grid;
    gap: 0.75rem;
  }

  .dialog-close {
    justify-self: end;
    border: 1px solid var(--border);
    border-radius: 999px;
    background: var(--bg);
    color: var(--text);
    padding: 0.3rem 0.75rem;
    cursor: pointer;
  }

  .dialog-highlights {
    margin-top: 0;
  }

  .stack-title {
    font-size: 0.9rem;
    color: var(--muted);
  }

  .tech {
    list-style: none;
    margin: 0.55rem 0 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem;
  }

  .tech li {
    border: 1px solid var(--border);
    border-radius: 999px;
    padding: 0.2rem 0.55rem;
    font-size: 0.8rem;
  }
</style>